# Mnemora v1.25.0 — Query Persistence Seam

## Internal reliability work

- Query watch persistence, digest idempotency and reclaim state, and query
  audit retention now live in a dedicated internal repository.
  `GraphologyStore` keeps its existing public methods and observable
  behavior.
- The repository keeps the established scope-touching, transaction, bounded
  listing, cursor, plan-hashing, audit-redaction, and daily retention rules
  together, so callers do not need SQLite lifecycle knowledge.

## Decision-gate status

- Real scope-local review has no pending vocabulary, duplicate, or self-link
  action. Existing semantic-pattern proposals are already accepted.
- No PPR policy change, recall canary, graph mutation, or automatic review
  action is justified or enabled by this release.

## Compatibility

- No database migration, configuration default, or agent-tool contract
  changed. Existing query, watch, digest, and audit behavior is preserved.
