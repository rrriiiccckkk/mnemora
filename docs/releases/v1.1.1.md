# Mnemora v1.1.1

## Test-suite repair

Fixes seven v1.1.0 test-only migration assertions that referenced an undeclared
`migrated` variable after the schema `61 → 62` update. The affected tests now
correctly inspect their existing `store` instance.

This release does not change production code, configuration, runtime behavior,
or the schema. It restores complete test-suite signal for consolidation,
ContextEngine migration, node-importance, and semantic-boundary coverage.

## Verification

Validated on Node.js 24 with the full unit suite, the seven repaired regression
paths, build, and release-version consistency checks.
