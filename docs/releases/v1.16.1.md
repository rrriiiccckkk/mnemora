# Mnemora v1.16.1 — Semantic Vocabulary Test Stability

## Validation-only patch

- Fixes a nondeterministic v1.16 semantic-vocabulary regression test. The
  test now queries the source and target of the exact per-edge candidate it
  confirms, rather than assuming a hash-ordered candidate belongs to one
  fixture entity.
- No production behavior, schema, configuration, graph topology, review
  workflow, or recall policy changes.

## Validation

- Reproduced the previous flaky assertion with a 20-run focused loop, then
  verified the repaired loop passes 20 consecutive runs and the complete
  semantic-vocabulary test file passes.
