let csrf="";
export async function bootstrap():Promise<void>{const params=new URLSearchParams(location.hash.slice(1)),token=params.get("bootstrap");if(!token)throw new Error("missing bootstrap token");const response=await fetch("/api/bootstrap",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token})});if(!response.ok)throw new Error("bootstrap failed");csrf=String((await response.json()).csrf??"");history.replaceState(null,"",location.pathname+location.search);}
export async function api<T>(path:string,input:unknown={}):Promise<T>{const operation=path.startsWith("/api/operations/");const response=await fetch(path,{method:"POST",credentials:"same-origin",headers:{"content-type":"application/json",...(operation?{"x-csrf-token":csrf}:{})},body:JSON.stringify(input)});if(!response.ok)throw new Error(`request failed: ${response.status}`);return response.json() as Promise<T>;}
