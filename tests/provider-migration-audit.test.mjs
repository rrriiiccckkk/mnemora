import assert from "node:assert/strict";
import test from "node:test";
import { GraphologyStore, ProviderMigrationAuditRepository, SUPPORTED_SCHEMA_VERSION } from "../dist/index.js";
test("provider migration audits are additive, scope-bound, and source-hash idempotent",()=>{const store=new GraphologyStore(":memory:");try{assert.equal(SUPPORTED_SCHEMA_VERSION, 74);const repo=new ProviderMigrationAuditRepository(store.db),source={ref:{provider:"memory-lancedb-pro",externalId:"m1"},content:"safe import",contentHash:"ignored"},first=repo.record({provider:"memory-lancedb-pro",scope:"work",source,status:"imported",cursor:"m1"}),second=repo.record({provider:"memory-lancedb-pro",scope:"work",source,status:"imported",cursor:"m1"});assert.equal(repo.list("memory-lancedb-pro","work").length,1);assert.equal(repo.list("memory-lancedb-pro","other").length,0);assert.equal(first.contentHash,second.contentHash);}finally{store.close();}});
